---
description: Branching, merge-to-main, and sync workflow for this repo
alwaysApply: true
---

# Git workflow

- **Before changing code** (at the start of a task that will touch the repo): make sure `main` is up to date (`git checkout main` → `git pull`), then create and check out a dedicated branch (for example `feature/short-description`). Do the work on that branch, not on `main`.
- **New features / any multi-file work**: same rule — branch first, then implement, then commit on the branch.
- **When the user says “merge”** (or clearly asks to land the current feature on main): merge the current feature branch into `main`, switch to `main`, run `git pull` to sync with the remote, and push `main` if the merge was local-only or not yet on the remote.
- Prefer fast-forward or explicit merge commits according to what the user prefers; default to a normal merge if unspecified.
